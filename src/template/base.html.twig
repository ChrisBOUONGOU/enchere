<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="{{ asset('css/main.css') }}">  
    <script src="{{ asset('js/count.js') }}"></script>  
    <title>{% block title %}Bienvenue{% endblock %}</title>
</head>
<body>
    <header class="p-3 mb-3 border-bottom">
        <div class="container">
            <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 link-body-emphasis text-decoration-none">
                    <img src="{{ asset('img/encho.jpg') }}" height="50" width="50">
                </a>

                <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
                <li><a href="{{ path('home') }}" class="d-flex align-items-center px-2 text-white text-decoration-none">Home</a></li>
                <li><a href="{{ path('contact') }}" class="d-flex align-items-center px-2 text-white text-decoration-none">Contact</a></li>
                <li><a href="{{ path('app_product') }}" class="d-flex align-items-center px-2 text-white text-decoration-none">Products</a></li>
                </ul>

                {% if not app.user %}
                <ul class="nav">
                  <li class="nav-item"><a href="{{ path('security_login') }}" class="d-flex align-items-center px-2 text-white text-decoration-none">Login</a></li>
                  <li class="nav-item"><a href="{{ path('security_reg') }}" class="d-flex align-items-center px-2 text-white text-decoration-none">Sign up</a></li>
                </ul>
                {% else %}
                <ul class="nav"> 
                        <li class="nav-item"><a href="{{ path('card_index') }}" class="d-flex align-items-center px-2 text-white text-decoration-none">Panier</a></li>                      
                        <li class="nav-item"><a href="{{ path('security_logout') }}" class="d-flex align-items-center px-2 text-white text-decoration-none">Sign out</a></li>
                </ul>
               {% endif %}

            </div>
        </div>
    </header>
    <div class="container">
    {% block body %}{% endblock %}
    </div>
        
    <footer class="blog-footer d-flex flex-wrap justify-content-between align-items-center py-3">
    <div class="col-md-4 d-flex align-items-center">
    
      <a href="/" class="d-flex align-items-center px-2 text-white text-decoration-none">
        <svg class="bi" width="30" height="24"><use xlink:href="#bootstrap"/></svg>
      </a>
      <span class="d-flex align-items-center px-2 text-white text-decoration-none">&copy; 2023 Company, Inc</span>
    </div>

    <ul class="nav col-md-4 justify-content-end list-unstyled d-flex">
      <li class="ms-3"><a class="text-body-secondary" href="#"><svg class="bi" width="24" height="24"><use xlink:href="#twitter"/></svg></a></li>
      <li class="ms-3"><a class="text-body-secondary" href="#"><svg class="bi" width="24" height="24"><use xlink:href="#instagram"/></svg></a></li>
      <li class="ms-3"><a class="text-body-secondary" href="#"><svg class="bi" width="24" height="24"><use xlink:href="#facebook"/></svg></a></li>
    </ul>
  </footer>
 



</body>
</html>